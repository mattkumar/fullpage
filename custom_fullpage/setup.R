# Libs
library(shiny)
library(fullPage)
library(highcharter)

# Options for full Page
options <- list(
  sectionsColor = c('#d11141',
                    '#00b159', 
                    '#00aedb', 
                    '#f37735', 
                    '#ffc425',
                    '#fff'),
  parallax = TRUE,
  navigation = TRUE,
  navigationPosition = 'right',
  fadingEffect = TRUE,
  controlArrows = TRUE
)


 