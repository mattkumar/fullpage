# Libs
library(shiny)
library(fullPage)

# Options for full Page
options <- list(
  sectionsColor = c('#d11141',
                    '#00b159', 
                    '#00aedb', 
                    '#f37735', 
                    '#ffc425',
                    '#fff'),
  parallax = TRUE,
  navigation = TRUE,
  fadingEffect = TRUE
)


 